# Changelog

## [Unreleased]

### Added

- **WEB-1 scaffold.** Next.js 15 App Router workspace with Cloudflare OpenNext config (no D1). Design tokens and empty chrome for `/` and `/tools/flow-linter`. `@sfdt/flow-core` is a dependency; `GET /api/engine` reports its version. Org data is not stored.
